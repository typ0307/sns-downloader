name: Keep Streamlit app awake

on:
  schedule:
    - cron: "0 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping app health endpoint
        run: curl -fsSL --retry 3 --max-time 30 https://sns-downloader.streamlit.app/_stcore/health
